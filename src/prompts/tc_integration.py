# -*- coding: utf-8 -*-
"""TC-INTEGRATION - 集成测试用例 Prompt（ASIL差异化 + FMEA/DFA 故障注入导出）"""

from prompts._base import get_asil_requirements


def build_tc_integration_prompt(code: str, module_name: str, asil_level: str) -> str:
    """构建 TC-INTEGRATION 文档生成 Prompt。"""
    asil_req = get_asil_requirements(asil_level)
    return f"""你是一名功能安全测试工程师，请根据以下 C/C++ 嵌入式代码，设计完整的**集成测试用例文档（TC-INTEGRATION）**，符合 ISO 26262 Part 6 软件集成测试要求和 ASPICE SWE.5 集成测试规范。

## 模块名称
{module_name}

## ASIL 等级
{asil_level}

## 本 ASIL 等级的 ISO 26262 Part 6 方法要求
{asil_req}

## 源代码
```c
{code}
```

## 输出格式要求

请按照以下结构输出 Markdown 格式的集成测试用例文档：

# {module_name} 集成测试用例文档（TC-INTEGRATION）

## 1. 集成测试策略
简述集成测试方法（自顶向下 / 自底向上 / 三明治法）、测试环境假设（含硬件在环 HIL / 软件在环 SIL）、Mock/Stub 策略。

## 2. 接口分析
### 2.1 模块内部接口
分析代码中各函数/子模块之间的调用关系和数据流，绘制接口调用图。
### 2.2 外部接口
分析模块与外部系统（驱动层、中间件、总线通信）的接口依赖关系。

## 3. 集成测试用例表

请使用以下测试设计技术设计集成测试用例：

### 3.1 接口调用链测试
### 3.2 数据流集成测试
### 3.3 控制流集成测试
### 3.4 时序与并发测试
### 3.5 故障注入集成测试（FMEA/DFA 导出）
从前置 FMEA 和 DFA 文档中导出接口级故障注入用例：
- 针对 FMEA 中涉及接口/通信的失效模式，注入总线错误、报文丢失、超时、数据篡改等故障
- 针对 DFA 中的级联失效路径，验证故障不会跨模块传播
- 验证 FFI（无干扰）属性：某模块故障不影响其他模块的正常运行

测试用例表格格式：

| 测试用例ID | 测试技术 | 被测接口/调用链 | 测试目的 | 前置条件 | 输入数据 | 预期结果 | 优先级 | 追溯SRS | 追溯FMEA |
|-----------|---------|--------------|---------|---------|---------|---------|--------|--------|----------|

要求：
- **测试用例ID**：格式 IT-MOD-XXX（XXX为三位序号，必须连续）
- **优先级**：高(安全相关) / 中(功能正确性) / 低(边界/异常)
- **追溯SRS**：关联到 SRS / SAD 需求ID，必须引用前置文档中实际存在的编号
- **追溯FMEA**：关联到 FMEA 失效模式ID（FM-MOD-XXX）或 DFA 失效ID（CCF-XXX/CAS-XXX），故障注入类必填

## 4. 集成测试代码框架
### 4.1 使用 CMock/FFI 的集成测试代码
### 4.2 使用 Google Mock 的集成测试代码

## 5. 测试覆盖矩阵
集成测试用例与接口/调用链的覆盖矩阵表。

## 6. 测试结果记录模板

| 测试用例ID | 执行日期 | 执行者 | 实际结果 | 通过/失败 | 备注/缺陷ID |
|-----------|---------|--------|---------|----------|------------|

## 7. 测试通过准则
各 ASIL 等级对应的集成测试覆盖率要求、接口覆盖率要求、通过率要求。

请确保集成测试用例全面覆盖模块间的所有接口和关键调用路径，特别关注安全相关的集成场景。"""
